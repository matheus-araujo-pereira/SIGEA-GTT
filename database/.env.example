# =============================================================================
# SIGEA-GTT: Exemplo de Variáveis de Conexão com o Banco de Dados
# =============================================================================

# Configurações Padrão de Desenvolvimento Local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sigea_gtt
DB_USER=sigea_admin
DB_PASSWORD=sigea_dev_password

# Para conexão via URL única (compatível com Render / Heroku / AWS RDS)
# DATABASE_URL=postgresql://sigea_admin:sigea_dev_password@localhost:5432/sigea_gtt

